# OmniRhythm Development Task Tracker

## Milestone 1: Core Engine Refactoring
- [ ] Ticket 1.1: Interface Extraction
      * Define `IPlayableSong.cs` in `Core/Interfaces/`.
      * Encapsulate `MidiFileData` and `MidiPlayer` setup/teardown into `PianoSong.cs` (`Domains/Piano/`).
      * Move `PianoRenderer.RenderKeyboard()`, `ScreenCanvas.DrawPlaybackNotes()`, and input loop logic to `PianoSong.Draw()` and `.Update()`.
- [ ] Ticket 1.2: State Management & Main Loop
      * Replace raw MIDI global state with `public static IPlayableSong CurrentSong` in the main loop or `Application.cs`/`FileDialogs`.
      * Refactor `MidiPlaybackWindow.cs` to call `CurrentSong.Draw()` instead of hardcoded piano renderers.
      * Verify compilation and basic MIDI playback regression testing.

## Milestone 2: The Guitar Domain & Data Structures
- [ ] Ticket 2.1: Domain Models
      * Create `GuitarNote.cs`, `GuitarChord.cs`, `NoteTechnique.cs` (Flags enum) in `Domains/Guitar/Models/`.
      * Setup `GuitarSong.cs: IPlayableSong` container class.
- [ ] Ticket 2.2: Rocksmith CDLC Parsing
      * Implement `RocksmithParser.cs`. Integrate XML parsing (custom or via `Rocksmith2014.XML` packages).
      * Flatten arrangements to Level difficulty and construct `GuitarNote` collections.
- [ ] Ticket 2.3: Audio Extraction Pipeline
      * Use `System.Diagnostics.Process` to run `vgmstream-cli` against `_wem` files extracted from PSARC.
      * Integrate NAudio implementation into `GuitarSong` to play the decoded `.wav` file accurately.

## Milestone 3: The Guitar Renderer
- [ ] Ticket 3.1: ImGui 3D Projection Utility
      * Implement `ProjectToScreen(int stringIndex, float timeOffset, float yOffset)` inside `GuitarSong.cs`.
- [ ] Ticket 3.2: Base Highway Geometry
      * Render static elements of the guitar track (Fretboard width, string lines, colors, and HitLine).
- [ ] Ticket 3.3: Note & Sustain Rendering
      * Draw moving elements. Add notes using `AddRectFilled`. Map Time to Z depth, and use perspective math for sizes.
      * Optimize iteration logic (visible range culling).

## Milestone 4: Integration
- [ ] Ticket 4.1: File Browser Update
      * Expand file ingestion filtering to allow `.psarc`. Let `SongFactory` spin up appropriately.
- [ ] Ticket 4.2: Asynchronous Song Factory
      * Add async loading flag with ImGui loading message to prevent freezing.
      * Pass `.mid` paths to `new PianoSong()` and `.psarc` files to `await RocksmithParser.LoadAsync()`.
